# droidtv-remote configuration

# HTTP port for the PWA and API
server_port: 7503

# Add TVs and manage the common app-launcher library from the PWA.
# Existing tv_ip, tv_name, and apps values are migrated automatically once.
